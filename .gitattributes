# Normalize line endings
* text=auto

# Force LF for shell scripts (critical for Unix execution)
*.sh text eol=lf

# Audio files — treat as binary if added to the repo
*.wav binary
*.mp3 binary
*.aiff binary
*.m4a binary
